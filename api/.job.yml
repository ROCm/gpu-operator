---
version: 2.0

targets:
  api-test:
    commands: [ "/bin/sh", "-c", "echo hello world && exit 0" ]
    owners: ["email:mharkara@amd.com"]
    area:
    sub-area:

image:
  bind_dir: "/go/src/github.com/ROCm/gpu-operator"
  work_dir: "/go/src/github.com/ROCm/gpu-operator"
